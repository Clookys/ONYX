{
  "name": "onyx-render-web-service",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix onyx-landing && npm run build --prefix onyx-landing",
    "start": "node server/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.8",
    "express": "^4.21.2"
  }
}